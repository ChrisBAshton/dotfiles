# Configure Atom to show hidden files when doing File -> Open Project. (good for ~/.grunt-init/ projects)
defaults write com.github.atom AppleShowAllFiles -bool true

# actually, sod it - do the same for Finder
defaults write com.apple.finder AppleShowAllFiles YES
